class Solution {
public:
    int hammingWeight(uint32_t n) {
        int count = 0;
        uint32_t test;
        int i = 32;
        while(i--){
            test = n&1;
            if(test==1) count++;
            n = n>>1;
        }
        return count;
    }
};
